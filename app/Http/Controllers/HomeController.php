<?php

// namespace App\Http\Controllers;

// use Illuminate\Http\Request;
// use Inertia\Inertia;

// class HomeController extends Controller
// {
//     public function redirectUser(){
//         if(auth()->user()->hasRole('admin')){
//             // return redirect()->route('admin.dashboard');
//             return Inertia::render('Admin/Dashboard'); // this works but not what I want
//         }


//         if(auth()->user()->hasRole('teacher')){
//             return redirect()->route('teacher.dashboard');
//             return Inertia::render('Teacher/Dashboard'); // this works but not what I want
//         }

//         if(auth()->user()->hasRole('student')){
//             return redirect()->route('student.dashboard');
//             return Inertia::render('Student/Dashboard'); // this works but not what I want
//         }
        
        
//         // if(auth()->user()->hasRole('student')){
//         //     // return redirect()->route('student.dashboard');
//         //     return Inertia::render('Student/Dashboard'); // this works but not the best way
//         // }


//     }

}
